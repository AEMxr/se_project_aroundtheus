export default class UserInfo {
  constructor(profileNameElement, jobElement) {}
  getUserInfo() {
    // returns an object containing user information.
  }
  setUserInfo() {
    // takes new user data and adds it to page.
  }
}
