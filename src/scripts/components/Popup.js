export default class Popup {
  constructor(popup) {
    // this._popup = generic open and closes a popup
  }

  open() {
    // open popup
    // be called in the preexisting even handlers
  }

  close() {
    // close popup
  }

  _handleEscClose() {
    // stores the logic for closing the popup  by pressing the esc key
  }

  setEventListeners() {
    // adds a click event to close the popup
  }
}
